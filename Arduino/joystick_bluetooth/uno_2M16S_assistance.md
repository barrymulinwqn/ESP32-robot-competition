# 2M16S & Arduino UNO（Digital PWM UNO）针脚与接口说明

> 参考图片：`2M16S_final.jpg` 和 `uno_final.jpg`

---

## 目录

- [一、2 Motor && 16 Servo Drive Board（2M16S）](#一2-motor--16-servo-drive-board2m16s)
  - [1.1 板卡概览示意图](#11-板卡概览示意图)
  - [1.2 各接口与针脚详解](#12-各接口与针脚详解)
    - [① 电源接口（Power）](#-电源接口power)
    - [② 电机驱动输出（Motor Output）](#-电机驱动输出motor-output)
    - [③ 16路舵机接口（Servo 1 ~ 16）](#-16路舵机接口servo-1--16)
    - [④ 与 Arduino UNO 通信接口](#-与-arduino-uno-通信接口)
- [二、Arduino UNO（Digital PWM UNO）](#二arduino-unodigital-pwm-uno)
  - [2.1 板卡概览示意图](#21-板卡概览示意图)
  - [2.2 各接口与针脚详解](#22-各接口与针脚详解)
    - [① 数字 I/O 引脚（D0 ~ D13）](#-数字-io-引脚d0--d13)
    - [② 模拟输入引脚（A0 ~ A5）](#-模拟输入引脚a0--a5)
    - [③ 电源引脚](#-电源引脚)
    - [④ USB-B 接口（固件烧录 & 串口通信）](#-usb-b-接口固件烧录--串口通信)
    - [⑤ ISP 六针烧录接口](#-isp-六针烧录接口)
- [三、固件烧录方法](#三固件烧录方法)
  - [3.1 Arduino UNO 固件烧录](#31-arduino-uno-固件烧录)
    - [方法一：USB-B 接口（推荐）](#方法一usb-b-接口推荐最简单)
    - [方法二：ISP 六针接口（需外部编程器）](#方法二isp-六针接口需外部编程器)
  - [3.2 2M16S 固件烧录](#32-2m16s-固件烧录)
- [四、完整接线汇总](#四完整接线汇总)
  - [4.1 系统架构总览](#41-系统架构总览)
  - [4.2 供电电路](#42-供电电路)
  - [4.3 完整接线图（ASCII 系统图）](#43-完整接线图ascii-系统图)
  - [4.4 引脚分配汇总表](#44-引脚分配汇总表)
    - [2M16S 对外连接汇总](#2m16s-对外连接汇总)
    - [Digital PWM UNO GPIO 分配汇总](#digital-pwm-uno-gpio-分配汇总)
  - [4.5 接线步骤（Step-by-Step）](#45-接线步骤step-by-step)
- [五、注意事项](#五注意事项)

---

## 一、2 Motor && 16 Servo Drive Board（2M16S）

### 1.1 板卡概览示意图

> 以下示意图根据 `2M16S_final.jpg` 实物核对绘制，反映各接口在板上的实际方位。

```
         顶部边缘 ──────────────────────────────────────────────────────────────────────
         ┌──────────────────────────────────────────────────────────────────────────────┐
         │   [S1] [S2] [S3] [S4] [S5] [S6] [S7] [S8]  舵机接口 1~8（顶部排，左→右）   │
         │   [S9][S10][S11][S12][S13][S14][S15][S16]   舵机接口 9~16（顶部排，左→右）  │
         │   每路 3 针：GND / VCC(5V) / SIG                                            │
         │                                                                              │
左边缘   │  [VIN][GND]  ← 外部电源输入（7~12V DC）                                    │  右边缘
         │                                                                              │
         │  [MA+][MA-]  ← Motor A 输出                    ┌──────────────┐            │
         │  [MB+][MB-]  ← Motor B 输出                    │  ATmega328P  │            │
         │                                                 │   (MCU)      │            │
         │                                                 └──────────────┘            │
         │                                                                              │
         │  与UNO通信引脚（SoftwareSerial）：                                          │
         │  [D0/RX] [D1/TX]  ← 运行时：D1(TX)→UNO Pin4，D0(RX)←UNO Pin5         │
         │                                                                              │
         │  ┌─────────────────────────────────────────────────────────────────┐        │
         │  │  🔴 固件烧录引脚（UART Header，独立排针，靠近 MCU）              │        │
         │  │      实物确认引脚（从左到右共 4 针）：                           │        │
         │  │                                                                  │        │
         │  │   ● GND   ← 接 USB-to-TTL 适配器 GND（必须）                   │        │
         │  │   ● 5V    ← 接 USB-to-TTL 适配器 5V（可选，板已独立供电时跳过） │        │
         │  │   ● TX    ← 接 USB-to-TTL 适配器 RX（2M16S发 → 适配器收）       │        │
         │  │   ● RX    ← 接 USB-to-TTL 适配器 TX（适配器发 → 2M16S收）       │        │
         │  │                                                                  │        │
         │  │   ⚠️ UART Header 无 RST 引脚！复位须手动按板上 RST 按钮         │        │
         │  └─────────────────────────────────────────────────────────────────┘        │
         │                                                                              │
         └──────────────────────────────────────────────────────────────────────────────┘
         底部边缘 ──────────────────────────────────────────────────────────────────────

  ⚠️ 板上无 USB-B 接口，无 ISP Header，固件烧录必须通过 UART Header（TX/RX/GND/5V）+
     USB-to-TTL 串口适配器完成，不能通过叠插 UNO 的 USB-B 接口烧录。
     UART Header 无 RST 引脚，烧录时须手动按 RST 按钮触发复位。
```

### 1.2 各接口与针脚详解

#### ① 电源接口（Power）

| 针脚 | 标识 | 说明 |
|------|------|------|
| 正极 | VIN  | 外部电源输入，7～12V DC（推荐 7.4V 锂电或 9V 适配器） |
| 负极 | GND  | 电源地（与逻辑地共地） |
| 板载输出 | 5V | 板载稳压输出，供舵机 VCC 使用（最大约 3A） |

> ⚠️ 若舵机数量多、电流大，建议外接独立 5V BEC 为舵机供电，避免烧毁稳压芯片。

#### ② 电机驱动输出（Motor Output）

| 接口 | 引脚 | 控制引脚（2M16S内部） | 说明 |
|------|------|:-------------------:|------|
| Motor 1（左电机） | M1A、M1B | D4(DIR A)、D5(DIR B)、D6(PWM) | 控制 M1 直流电机，可正反转（H 桥） |
| Motor 2（右电机） | M2A、M2B | D7(DIR A)、D8(DIR B)、D9(PWM) | 控制 M2 直流电机，可正反转（H 桥） |

- 驱动芯片：**L293DD**（板载 H 桥，DIP 封装）
- 单路额定电流：**600 mA 连续 / 1.2A 峰值**（⚠️ 33GB-520 启动电流可能超出，必要时外接 TB6612 大功率模块）
- 电机转速 / 方向由板载 ATmega328P 内部 DIR + PWM 信号控制，**外部接线仅需连接 M1A/M1B 和 M2A/M2B 四个输出端**

#### ③ 16路舵机接口（Servo 1 ~ 16）

每个舵机接口为 **3针排针**（从左到右）：

```
  GND  ●
  VCC  ●  (5V)
  SIG  ●  (PWM 信号，50Hz)
```

| 编号 | 功能 |
|------|------|
| S1 ~ S8  | 舵机通道 1～8 |
| S9 ~ S16 | 舵机通道 9～16 |

> 信号为标准 **50Hz PWM**，脉宽 500μs～2500μs 对应舵机 0°～180°。

#### ④ 与 Arduino UNO 通信接口

| 接口 | 2M16S 引脚 | UNO 引脚 | 说明 |
|------|:----------:|:--------:|------|
| SoftwareSerial TX | D1 (TX) | Pin 4 | 2M16S 发手柄状态帧 → UNO 接收（SoftSerial RX） |
| SoftwareSerial RX | D0 (RX) | Pin 5 | UNO 回传命中次数 → 2M16S（可选，SoftSerial TX） |
| 逻辑供电 | 5V | 5V | 为 UNO 及激光外设供电（可选，若 UNO 独立供电则跳过） |
| 共地 | GND | GND | 必须连接，否则 UART 电平偏移 |

- 通信协议：**SoftwareSerial，115200 bps，5V TTL 直连，无需电平转换**
- **本项目不使用 I2C（A4/A5 未占用）**；16 路舵机接口（PCA9685，I²C 0x40）留作扩展
- **烧录 2M16S 固件前必须断开 D0/D1 外部连线**，否则外部信号干扰 Bootloader 握手导致上传失败

> ⚠️ **注意**：2M16S 拥有独立的 ATmega328P MCU，**必须单独烧录**，不能通过叠插 UNO 的 USB-B 接口一并写入。根据实物版本不同，烧录方式有两种：若板上有 **USB-B（CH340）**，可直接插 USB 线烧录；若无 USB-B，则需使用 **USB-to-TTL 串口适配器**连接板上 **UART Header（TX/RX/GND/5V）** 引脚，配合 optiboot Bootloader 上传固件，且须手动按 RST 按钮触发复位。详见第三章 3.2 节。

---

## 二、Arduino UNO（Digital PWM UNO）

### 2.1 板卡概览示意图

```
                ┌─────────────────────────────────────────────────────────┐
                │                  Arduino UNO (Digital PWM UNO)          │
                │                                                         │
  USB-B 供电/   │ ┌──────┐                                               │
  固件烧录 ─────►│ │USB-B │   (内置 CH340 作 USB-串口桥接)                │
                │ └──────┘                                               │
                │                                                         │
  DC 电源 ──────►│ [DC 2.1mm 插座]  7～12V                               │
                │                                                         │
                │  数字 IO：D0(RX) D1(TX) D2~D13                         │
                │  其中 PWM：D3★  D5★  D6★  D9★  D10★  D11★            │
                │                                                         │
                │  模拟 IO：A0 A1 A2 A3 A4(SDA) A5(SCL)                 │
                │                                                         │
                │  电源针脚：3.3V / 5V / GND / VIN                       │
                │                                                         │
                │  ISP Header（6-pin）：用于直接 AVR 编程                  │
                │  RST 按钮：手动复位                                      │
                └─────────────────────────────────────────────────────────┘
```

### 2.2 各接口与针脚详解

#### ① 数字 I/O 引脚（D0 ~ D13）

| 引脚 | 特殊功能 | 说明 |
|------|---------|------|
| D0 (RX) | UART 接收 | 与 USB 串口共用；本项目中未接外部设备，烧录时无干扰 |
| D1 (TX) | UART 发送 | 与 USB 串口共用；本项目中未接外部设备，烧录时无干扰 |
| D2  | 外部中断 INT0 | 数字输入/输出 |
| D3  ★ | PWM + INT1 | PWM 输出（490Hz） |
| D4  | — | 数字输入/输出 |
| D5  ★ | PWM | PWM 输出（980Hz） |
| D6  ★ | PWM | PWM 输出（980Hz） |
| D7  | — | 数字输入/输出 |
| D8  | — | 数字输入/输出 |
| D9  ★ | PWM | PWM 输出（490Hz） |
| D10 ★ | PWM / SS  | PWM 输出（490Hz）/ SPI 片选 |
| D11 ★ | PWM / MOSI | PWM 输出（490Hz）/ SPI 数据 |
| D12 | MISO | SPI 数据输入 |
| D13 | SCK / LED | SPI 时钟 / 板载 LED |

> ★ 表示支持 PWM 输出的引脚

#### ② 模拟输入引脚（A0 ~ A5）

| 引脚 | 功能 | 说明 |
|------|------|------|
| A0 | 模拟输入 | 10-bit ADC，0～5V |
| A1 | 模拟输入 | 同上 |
| A2 | 模拟输入 | 同上 |
| A3 | 模拟输入 | 同上 |
| A4 | 模拟输入 / I2C SDA | 本项目中仅用作模拟输入，未使用 I2C |
| A5 | 模拟输入 / I2C SCL | 本项目中仅用作模拟输入，未使用 I2C |

> ⚠️ **本项目 UNO 与 2M16S 之间通过 SoftwareSerial（Pin 4 RX / Pin 5 TX）通信，并非 I2C。** A4/A5 在本项目中未使用。

#### ③ 电源引脚

| 针脚 | 说明 |
|------|------|
| 5V  | 输出 5V（由 USB 或板载稳压提供，最大 500mA） |
| 3.3V | 输出 3.3V（最大 50mA） |
| GND | 地（多个） |
| VIN | 外部电源输入（7～12V），与 DC 座共享 |
| RESET | 复位引脚（低电平触发） |

#### ④ USB-B 接口（固件烧录 & 串口通信）

- 连接电脑后通过 **Arduino IDE** 自动识别为串口（COMx / /dev/ttyUSB0 / /dev/tty.usbserial-xxx）
- 内置自动复位电路，烧录时 IDE 自动拉低 DTR 触发复位，无需手动按 RST
- 通信协议：**115200 baud**（默认）
- **本固件使用 SoftwareSerial（Pin 4 / Pin 5）与 2M16S 通信**，硬件 UART（D0/D1）保留给 USB 调试，**烧录时无需断开任何外部连线**

#### ⑤ ISP 六针烧录接口

引脚定义如下：

```
  ┌──────────────────┐
  │ MISO  VCC        │
  │ SCK   MOSI       │
  │ RST   GND        │
  └──────────────────┘
```

用于外部编程器（如 USBasp、AVRISP mkII）绕过 USB bootloader 直接烧录。

---

## 三、固件烧录方法

### 3.1 Arduino UNO 固件烧录

#### 方法一：USB-B 接口（推荐，最简单）

> ⚠️ **macOS 用户须先安装 CH340 驱动**：macOS 12（Monterey）及以上版本默认不含 CH340 驱动，接上 USB-B 线后端口不会出现。请先从 WCH 官网下载并安装 `CH34x_Install_V1.x.pkg`（[下载地址](https://www.wch-ic.com/downloads/CH341SER_MAC_ZIP.html)），安装后重启系统再连接 UNO。

1. 用 **USB-A to USB-B** 数据线连接 UNO 与电脑
2. 打开 **Arduino IDE**
3. 选择：
   - 菜单 `工具 → 开发板 → Arduino AVR Boards → Arduino Uno`
   - 菜单 `工具 → 端口` → 选择对应串口（如 `/dev/tty.usbserial-xxxx` 或 `COMx`）
4. 打开固件文件 `UNO_firmware/UNO_firmware.ino`
5. 点击 **上传（→）** 按钮，等待 "Done uploading" 提示

```
电脑 USB-A  ──── USB-B 数据线 ────►  UNO USB-B 接口
                                     ↓
                        IDE 拉低 DTR → 自动复位 → 激活 Bootloader → 写入固件
```

> ✅ **无需断开任何连线**：本固件使用 SoftwareSerial（Pin 4/5），D0/D1 不被占用，连着外部电路也可正常烧录。

#### 方法二：ISP 六针接口（需外部编程器）

1. 使用 **USBasp** 或 **AVRISP mkII** 编程器
2. 将编程器 6针 ISP 线连接至 UNO 板上的 ISP Header
3. Arduino IDE 选择：
   - `工具 → 编程器 → USBasp`（或对应型号）
   - `草图 → 通过编程器上传`（Ctrl+Shift+U）

---

### 3.2 2M16S 固件烧录

#### 📋 烧录方式选择说明

经核对 `2M16S_final.jpg`，2M16S 板上：
- ❌ **无 USB-B 接口**：无法像 UNO 一样直接插 USB 线烧录
- ❌ **无 ISP Header**：无法使用 USBasp/AVRISP mkII 等编程器
- ❌ **无法通过叠插 UNO 的 USB-B 接口烧录**：UNO 的 USB-B 只能访问 UNO 自身的 ATmega328P，**不能跨板写入 2M16S 上独立的 MCU**
- ✅ **唯一可行方法：USB-to-TTL 串口适配器**（CH340/CP2102/FT232 均可），通过板上 UART 引脚（TX/RX）配合 optiboot Bootloader 烧录

> ℹ️ **2M16S 出厂已预烧录 optiboot Bootloader**，支持通过串口 Bootloader 方式上传 `.ino` 固件，原理与 UNO 完全相同，只是没有板载 USB 桥接芯片，需外接 USB-to-TTL 适配器替代。

#### 为什么 USB-to-TTL 方法是唯一实用选择？

| 对比项 | 通过 UNO USB-B 烧录 2M16S | USB-to-TTL 串口适配器 |
|--------|--------------------------|---------------------|
| 是否可行 | ❌ **完全不可行**，UNO USB-B 只能烧录 UNO 自身 MCU | ✅ **唯一正确方法** |
| 所需硬件 | — | USB-to-TTL 适配器（CH340G 最常见，约 ¥5～¥30） |
| 操作难度 | — | 低，4 根杜邦线接好，步骤与烧录 UNO 相同 |
| 是否需断线 | — | 需暂时断开 2M16S 与 UNO 之间的 TX/RX 连线 |
| 自动复位 | — | ❌ UART Header 无 RST 引脚，**只能手动按板上 RST 按钮**触发复位 |

**结论：USB-to-TTL 串口适配器是烧录 2M16S 的唯一实用方法。**

---

#### ⚠️ 烧录前必须断开与 UNO 的连线

2M16S 的 **Pin 0（RX）/ Pin 1（TX）** 在运行时连接 UNO，烧录时这两根线会干扰 Bootloader 握手，**必须先断开**：

| 需断开的连线 | 说明 |
|------------|------|
| 2M16S Pin 1 (TX) ↔ UNO Pin 4 | 运行时发数据给 UNO，烧录时干扰 RX |
| 2M16S Pin 0 (RX) ↔ UNO Pin 5 | 运行时收 UNO 数据，烧录时干扰 TX |

#### 烧录步骤（USB-to-TTL 适配器）

1. **断开** 2M16S 与 UNO 之间 Pin 0/1 的连线
2. 按如下接线连接 USB-to-TTL 适配器与 2M16S：

| USB-TTL 适配器引脚 | 2M16S UART Header 引脚 | 说明 |
|------------------|----------------------|------|
| TX               | RX                   | 适配器发 → 2M16S 收 |
| RX               | TX                   | 2M16S 发 → 适配器收 |
| GND              | GND                  | 共地（必须连接） |
| 5V（可选）        | 5V                   | 若 2M16S 已独立供电则跳过 |

> ⚠️ **UART Header 无 RST 引脚**，无法通过 DTR 实现自动复位。烧录时须**手动按板上 RST 按钮**：在 Arduino IDE 点击上传后，观察到编译完成、进度条出现的瞬间，立即短按一下 2M16S 板上的 RST 按钮即可。

```
电脑 USB-A  ── USB-to-TTL 适配器 ──► TX → 2M16S UART Header RX
                                     RX ← 2M16S UART Header TX
                                     GND ── 2M16S UART Header GND
                                     （5V 可选，板已供电则跳过）
                                          ↓
              IDE 编译完成、进度条出现 → 手动按 2M16S RST 按钮
                                          ↓
                              激活 optiboot Bootloader → 写入固件
```

3. 打开 **Arduino IDE**，选择：
   - `工具 → 开发板 → Arduino AVR Boards → Arduino Uno`（ATmega328P @ 16 MHz）
   - `工具 → 端口` → 选择 USB-to-TTL 适配器对应的串口（如 `/dev/tty.usbserial-xxxx` 或 `COMx`）
4. 打开固件文件 `2M16S_firmware/2M16S_firmware.ino`
5. 安装依赖库：`工具 → 管理库` → 搜索并安装 **`PS2X_lib`**（by Bill Porter）
6. 点击 **上传（→）**，等待 "Done uploading"
7. 烧录完成后，**重新连接** 2M16S Pin 0/1 至 UNO Pin 5/4

---

## 四、完整接线汇总

### 4.1 系统架构总览

```
蓝牙游戏手柄（普通 BT 手柄）
        │ 无线蓝牙
        ▼
┌─────────────────────────────────────┐
│  YFRobot 2015 游戏手柄解码器        │
│  · 将蓝牙信号还原为 PS2 协议        │
│  · 引脚：GND / VCC / CLK / CS /     │
│          CMD / DAT                  │
└──────┬──────────────────────────────┘
       │ PS2 协议（CLK/CS/CMD/DAT，5V TTL）
       ▼
┌──────────────────────────────────────────────────────────────────┐
│          2 Motor && 16 Servo Drive Board（2M16S）                │
│  · PS2 硬连接（不可重映射）                                      │
│  · 内置 H 桥差速驱动双电机                                       │
│  · 硬件 UART D1(TX) → UNO Pin 4（SoftSerial RX）发送状态帧      │
└────────┬──────────────────────┬───────────────────────────────────┘
         │ H 桥直驱              │ UART TX（5V TTL，115200 bps）
    ┌────┴────┐                  ▼
    ▼         ▼      ┌────────────────────────────────────┐
左电机(M1)  右电机(M2)│  Digital PWM UNO                   │
33GB-520   33GB-520  │  · 接收 2M16S 手柄状态帧            │
DC 12V     DC 12V    │  · 激光发射控制（38 kHz PWM）       │
                     │  · VS1838B 激光命中检测              │
                     └──────────┬──────────────────────────┘
                                ├────────────────┐
                                ▼                ▼
                         激光发射器          VS1838B
                         980nm 30mW         激光接收器
                         38kHz TTL          38kHz 解调
```

---

### 4.2 供电电路

```
18650 电池组（3 节串联，~11.1V）
        │
        ├──[ON/OFF 拨动开关]──→ 2M16S  VIN（电机电源输入，6~18V）
        │                              │
        │                     2M16S 板载 5V 稳压（板载 LDO）
        │                       ├── 5V ──→ YFRobot 2015 VCC
        │                       ├── 5V ──→ Digital PWM UNO 5V
        │                       ├── 5V ──→ 激光发射器 VCC
        │                       └── 5V ──→ VS1838B VCC（2.7~5.5V 均可）
        │
        └──[负极]──→ 所有模块公共 GND 汇流点
```

> ⚠️ **所有模块 GND 必须短接至同一公共 GND 汇流点**，否则 UART 信号将出现电平偏移或误码。

---

### 4.3 完整接线图（ASCII 系统图）

```
  ┌─────────────────────────────────────────────────────────────────────────────┐
  │                 18650 电池组（3 节串联，~11.1V）                             │
  └────────────────────────┬────────────────────────────────────────────────────┘
                           │ [ON/OFF 开关]
                           ▼ VIN（~11.1V）
  ┌────────────────────────────────────────────────────────────────────────────┐
  │                2 Motor && 16 Servo Drive Board（2M16S）                    │
  │                        ATmega328P @ 16 MHz                                 │
  │                                                                            │
  │  ┌─── 电源 ───────────────────────────────────────────────────────────┐   │
  │  │  VIN ←──── 电池组正极（经开关，~11.1V）                            │   │
  │  │  GND ←──── 公共 GND 汇流点                                         │   │
  │  │  5V  ────► 板载稳压输出（供逻辑 + 外设，最大 ~500mA）              │   │
  │  └────────────────────────────────────────────────────────────────────┘   │
  │                                                                            │
  │  ┌─── PS2 手柄接口（硬连接，不可修改）────────────────────────────────┐   │
  │  │  D10 (CLK) ────────────────────────────► YFRobot 2015 CLK          │   │
  │  │  D11 (CS)  ────────────────────────────► YFRobot 2015 CS           │   │
  │  │  D12 (CMD) ────────────────────────────► YFRobot 2015 CMD          │   │
  │  │  D13 (DAT) ◄──────────────────────────── YFRobot 2015 DAT          │   │
  │  │  5V        ────────────────────────────► YFRobot 2015 VCC          │   │
  │  │  GND       ────────────────────────────► YFRobot 2015 GND          │   │
  │  └────────────────────────────────────────────────────────────────────┘   │
  │                                                                            │
  │  ┌─── 电机驱动输出（H 桥，板载 L293DD）──────────────────────────────┐   │
  │  │  M1A ──────────────────────────────────► 左电机 正极（红）         │   │
  │  │  M1B ──────────────────────────────────► 左电机 负极（黑）         │   │
  │  │  M2A ──────────────────────────────────► 右电机 正极（红）         │   │
  │  │  M2B ──────────────────────────────────► 右电机 负极（黑）         │   │
  │  │       控制引脚（内部已连接，无需外部接线）：                        │   │
  │  │         D4/D5 = M1 方向 A/B，D6(PWM) = M1 速度                    │   │
  │  │         D7/D8 = M2 方向 A/B，D9(PWM) = M2 速度                    │   │
  │  └────────────────────────────────────────────────────────────────────┘   │
  │                                                                            │
  │  ┌─── 硬件 UART 与 Digital PWM UNO 通信（D1/TX → UNO Pin4，115200 bps）┐  │
  │  │  D1 (TX) ──────────────────────────────► UNO Pin 4（SoftSerial RX） │  │
  │  │  D0 (RX) ◄──────────────────────────── UNO Pin 5（SoftSerial TX）  │  │
  │  │  5V      ──────────────────────────────► UNO 5V（为 UNO 供电）      │  │
  │  │  GND     ──────────────────────────────► UNO GND（共地）            │  │
  │  │  波特率：115200 bps，5V TTL 直连，无需电平转换                       │  │
  │  └─────────────────────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────────┐
  │                  Digital PWM UNO（Arduino UNO 兼容）                       │
  │                                                                            │
  │  ┌─── 与 2M16S 通信（SoftwareSerial）────────────────────────────────┐   │
  │  │  Pin 4（SoftSerial RX）◄──────────── 2M16S D1(TX)（手柄状态帧）   │   │
  │  │  Pin 5（SoftSerial TX）────────────► 2M16S D0(RX)（命中次数，可选）│   │
  │  └────────────────────────────────────────────────────────────────────┘   │
  │                                                                            │
  │  ┌─── 激光发射器接线 ──────────────────────────────────────────────────┐  │
  │  │  Pin 9（OC1A，Timer1）────────────────► 激光发射器 TTL/Signal       │  │
  │  │  5V ──────────────────────────────────► 激光发射器 VCC              │  │
  │  │  GND ─────────────────────────────────► 激光发射器 GND              │  │
  │  │  频率：38 kHz（ICR1=421, OCR1A=210），匹配 VS1838B 解调频率        │  │
  │  └─────────────────────────────────────────────────────────────────────┘  │
  │                                                                            │
  │  ┌─── VS1838B 激光接收器接线 ──────────────────────────────────────────┐  │
  │  │  Pin 2（INT0，外部中断）◄─────────────── VS1838B Pin1（OUT）        │  │
  │  │  5V ──────────────────────────────────► VS1838B Pin3（VCC）        │  │
  │  │  GND ─────────────────────────────────► VS1838B Pin2（GND）        │  │
  │  │  触发方式：下降沿（Active LOW），命中时中断计数递增                  │  │
  │  └─────────────────────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────────┐
  │                  YFRobot 2015 游戏手柄解码器                               │
  │           · 蓝牙信号 → PS2 协议（CLK/CS/CMD/DAT）                         │
  │                                                                            │
  │  CLK ──────────────────────────────────► 2M16S D10                       │
  │  CS  ──────────────────────────────────► 2M16S D11                       │
  │  CMD ──────────────────────────────────► 2M16S D12                       │
  │  DAT ◄─────────────────────────────────── 2M16S D13                      │
  │  VCC ◄─────────────────────────────────── 2M16S 5V                       │
  │  GND ──────────────────────────────────► 公共 GND                        │
  └────────────────────────────────────────────────────────────────────────────┘
```

---

### 4.4 引脚分配汇总表

#### 2M16S 对外连接汇总

| 2M16S 引脚 | 方向 | 连接至 | 说明 |
|:----------:|:----:|:------:|------|
| **VIN** | ← | 电池组正极（经开关，~11.1V） | 电机电源输入 |
| **GND** | — | 公共 GND 汇流点 | 必须连接 |
| **5V** | → | YFRobot VCC / UNO 5V / 激光 VCC / VS1838B VCC | 板载 5V 稳压输出 |
| **D10 (CLK)** | → | YFRobot 2015 **CLK** | PS2 时钟，硬连接 |
| **D11 (CS)** | → | YFRobot 2015 **CS** | PS2 片选，硬连接 |
| **D12 (CMD)** | → | YFRobot 2015 **CMD** | PS2 命令（主→手柄），硬连接 |
| **D13 (DAT)** | ← | YFRobot 2015 **DAT** | PS2 数据（手柄→主），硬连接，与 LED 共用 |
| **M1A** | → | 左电机 正极（红线） | H 桥输出 |
| **M1B** | → | 左电机 负极（黑线） | H 桥输出 |
| **M2A** | → | 右电机 正极（红线） | H 桥输出 |
| **M2B** | → | 右电机 负极（黑线） | H 桥输出 |
| **D1 (TX)** | → | UNO **Pin 4**（SoftwareSerial RX） | 手柄状态帧，115200 bps |
| **D0 (RX)** | ← | UNO **Pin 5**（SoftwareSerial TX） | 命中次数回传（可选） |

> ⚠️ **烧录 2M16S 固件前必须断开 D0/D1 的外部连线**，否则 USB 串口与外部信号争用会导致上传失败。

#### Digital PWM UNO GPIO 分配汇总

| UNO 引脚 | 方向 | 功能 | 连接设备 | 备注 |
|:--------:|:----:|:----:|:--------:|------|
| **Pin 2** | 输入 | 外部中断 INT0 | VS1838B OUT | Active LOW，下降沿触发命中计数 |
| **Pin 4** | 输入 | SoftwareSerial RX | 2M16S D1(TX) | 接收手柄状态帧，115200 bps |
| **Pin 5** | 输出 | SoftwareSerial TX | 2M16S D0(RX)（可选） | 回传命中次数 |
| **Pin 9** | 输出 | Timer1 OC1A PWM | 激光发射器 TTL/Signal | 38 kHz 载波（ICR1=421, OCR1A=210） |
| **Pin 0/RX** | — | 硬件 UART（保留） | USB 串口调试 | 烧录时勿外接信号 |
| **Pin 1/TX** | — | 硬件 UART（保留） | USB 串口调试 | 烧录时勿外接信号 |
| **5V** | 输出 | 电源 | 激光发射器 VCC，VS1838B VCC | 由 2M16S 5V 供给 |
| **GND** | — | 公共地 | 所有外设 | — |

> **通信说明**：UNO 使用 `SoftwareSerial`（Pin 4 RX / Pin 5 TX，115200 bps）接收 2M16S 发来的手柄状态帧，并回传激光命中计数。**两板之间不使用 I2C，A4/A5 在本项目中未占用。**

---

### 4.5 接线步骤（Step-by-Step）

**第一步：搭建供电电路（2 根线）**
1. 三节 18650 电池串联，总电压约 11.1V，正极串入 ON/OFF 拨动开关。
2. 正极（经开关）→ **2M16S VIN** 螺丝端子；负极 → **2M16S GND** 公共汇流点。

**第二步：YFRobot 2015 解码器 → 2M16S（6 根线）**
3. 解码器 **GND** → 2M16S **GND**
4. 解码器 **VCC** → 2M16S **5V**
5. 解码器 **CLK** → 2M16S **D10**
6. 解码器 **CS** → 2M16S **D11**
7. 解码器 **CMD** → 2M16S **D12**
8. 解码器 **DAT** → 2M16S **D13**（若信号不稳，DAT 与 VCC 间加 10kΩ 上拉电阻）

**第三步：2M16S → 电机（4 根线）**
9. **M1A** → 左电机正极（红）；**M1B** → 左电机负极（黑）
10. **M2A** → 右电机正极（红）；**M2B** → 右电机负极（黑）

> 若实测电机转向相反，互换该电机两根接线（如 M1A↔M1B），无需修改代码。

**第四步：2M16S → Digital PWM UNO（4 根线）**
11. 2M16S **D1(TX)** → UNO **Pin 4**（SoftwareSerial RX）
12. 2M16S **D0(RX)** ← UNO **Pin 5**（SoftwareSerial TX，可选）
13. 2M16S **5V** → UNO **5V**（为 UNO 供电）
14. 2M16S **GND** → UNO **GND**

**第五步：激光发射器 → UNO（3 根线）**
15. 激光模组 **VCC** → UNO **5V**
16. 激光模组 **GND** → 公共 GND
17. 激光模组 **TTL/Signal** → UNO **Pin 9**

**第六步：VS1838B → UNO（3 根线）**

> VS1838B 正面（圆弧面朝向目标），引脚从左到右：Pin1(OUT) / Pin2(GND) / Pin3(VCC)

18. VS1838B **Pin3(VCC)** → UNO **5V**
19. VS1838B **Pin2(GND)** → 公共 GND
20. VS1838B **Pin1(OUT)** → UNO **Pin 2**（INT0 外部中断，Active LOW）

---

## 五、注意事项

1. **共地（GND）必须连接**：UNO 与 2M16S 之间的 GND 必须相连，否则 SoftwareSerial 串口通信异常（电平参考错位）。
2. **电源分离**：建议大电流（电机/舵机）由独立电源供给 VIN，逻辑 VCC 可由 UNO 5V 提供。
3. **烧录前断开电机**：烧录固件时建议断开电机，防止误触发。
4. **ISP 接口方向**：ISP Header 针1通常以白色/三角形标记，接错会损坏器件。
5. **USB-B 烧录时勿占用 D0/D1**：这两个引脚与 USB 串口复用，会导致烧录失败。
